[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    slow: tests that take > 10 s (run with -m slow separately)
    integration: end-to-end tests requiring file I/O
